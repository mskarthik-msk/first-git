// import React from "react";
// import user from "./user";

// function sorting(user,id){
//     return array.sort((a, b) => {
//             if (a[id] < b[id] ) return -1;
//             if (a[id] > b[id] ) return 1;
//     })t
//     }
//   export function sorting(user, name){
//     return array.sort((a, b) => {
//             if (a[name] < b[name] ) return -1;
//             if (a[name] > b[name] ) return 1;
//     })
//     }
//   export function sorting(user, email){
//     return array.sort((a, b) => {
//             if (a[email] < b[email] ) return -1;
//             if (a[email] > b[email] ) return 1;
//     })
//     }
//   export function sorting(user, phone){
//     return array.sort((a, b) => {
//             if (a[phone] < b[phone] ) return -1;
//             if (a[phone] > b[phone] ) return 1;
//     })
//     }
//   export function sorting(user,company.name){
//     return array.sort((a, b) => {
//             if (a[company.name] < b[company.name] ) return -1;
//             if (a[company.name] > b[company.name] ) return 1;
//     })
//     }
//   export function sorting(User,website){
//     return array.sort((a, b) => {
//             if (a[website] < b[website] ) return -1;
//             if (a[website] > b[website] ) return 1;
//     })
//     }
  
//   export default sorting;
// switch (sortype) {
//     case 0:
//         let sortId = user.sort();
//         setSortname(sortId);
//     case 1:
//         let sortName = user.sort((a, b) => {
//             if (a.name < b.name) return -1;
//             if (a.name > b.name) return 1;
//         });
//         setSortname(sortName);
//     case 2:
//         let sortEmail = user.sort((a, b) => {
//             if (a.email < b.email) return -1;
//             if (a.email > b.email) return 1;
//         });
//         setSortname(sortEmail);
//     case 3:
//         let sortPhone = user.sort((a, b) => {
//             if (a.phone < b.phone) return -1;
//             if (a.phone > b.phone) return 1;
//         });
//         setSortname(sortPhone);
//     case 4:
//         let sortCompany = user.sort((a, b) => {
//             if (a.company.name < b.company.name) return -1;
//             if (a.company.name > b.company.name) return 1;
//         });
//         setSortname(sortCompany);
//     case 5:
//         let sortWebsite = user.sort((a, b) => {
//             if (a.website < b.website) return -1;
//             if (a.website > b.website) return 1;
//         });
//         setSortname(sortWebsite);
//   }